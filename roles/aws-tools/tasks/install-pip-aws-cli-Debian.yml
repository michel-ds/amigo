---
- name: Install pip3
  apt: name=python3-pip state=present

- name: Install latest AWS CLI
  shell: |
    pip3 --version
    echo Installing pip3 install -I awscli==1.19.12
    pip3 install -I awscli==1.19.12
    echo Installed. Listing
    pip3 show awscli